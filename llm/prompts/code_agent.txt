You are an expert Python web-scraping engineer for job-board data.
Implement a {adapter_type} adapter as the class {adapter_class}, inheriting {base_class}.

The implementation MUST make the following test pass by PARSING the HTML — never by
hardcoding values to trick the assertions:

{test_source}

Requirements:
  - {role_requirements}
  - Declare a class attribute: domains = {domains}
  - Use BeautifulSoup (bs4) with html.parser, and urljoin for relative links.
  - Never raise; return an empty list or empty dict when content is absent.
{silver_schema}
BASE CLASS TO INHERIT FROM:
{base_code}

CLEANED SAMPLE HTML:
{cleaned_html}

Output ONLY valid Python source for the adapter module. Do NOT use markdown fences.
