You are an expert Python web scraping engineer specialising in job board data extraction.
Your task is to generate a production-quality Python adapter for the domain: {domain}

==========================================================================
ADAPTER TYPE: {adapter_type}
==========================================================================

{base_class_section}

==========================================================================
SILVER SCHEMA — what an ExtractionAdapter must return
==========================================================================

The extract() method must return a Python dict matching this schema.
All keys are required unless marked optional.

{
    "title":           str,          # job title, e.g. "Senior Software Engineer"
    "company_name":    str,          # employer name, e.g. "Anthropic"
    "employment_type": str | None,   # "Full-time", "Part-time", "Contract", or None
    "locations":       list[str],    # e.g. ["San Francisco, CA", "Remote"]
    "categories":      list[str],    # e.g. ["Engineering", "Machine Learning"]
    "description":     str,          # full job description as plain text or Markdown
    "metadata":        dict,         # any extra fields (can be empty {})
}

==========================================================================
SAMPLE HTML FROM TARGET DOMAIN
==========================================================================

{html_snippet}

==========================================================================
BASE CLASS TO INHERIT FROM
==========================================================================

{base_code}

==========================================================================
BASE TEST CLASS YOUR TEST MUST INHERIT FROM
==========================================================================

{test_base_code}

==========================================================================
REQUIREMENTS
==========================================================================

1. Inherit ONLY from the base class shown above. Do NOT create new base classes.
2. Implement ALL abstract methods completely — no stubs, no `pass`, no `...`.
3. Use BeautifulSoup (bs4) with html.parser for HTML parsing.
4. Use urljoin() from urllib.parse to resolve relative URLs.
5. Return empty list / empty dict when content is absent — never raise exceptions.
6. Declare a class attribute: domains = ["{domain}"] (add www. variant if applicable).
7. Output ONLY valid Python source. Do NOT wrap code in markdown fences.
8. Separate the adapter class and the test code with this exact line:
   # --- TEST CODE ---
9. The test file must inherit from the provided test base class and implement all
   abstract test fixture methods (get_adapter, get_html_with_jobs, etc.).
10. Import the test base class as:
    from adapters.adapters.base_test import {test_base_class}

==========================================================================
{refinement_section}
